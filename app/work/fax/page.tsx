/* Canonical case study. Clean, no version switcher.
   The component lives in components/casestudy alongside the Staging one, and both
   render from components/casestudy/kit.tsx. */
export { default } from "@/components/casestudy/FaxCaseStudy";
