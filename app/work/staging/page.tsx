/* Canonical case study (current). Clean, no version switcher.
   The component lives in components/casestudy so /work/staging/v2 can reuse it. */
export { default } from "@/components/casestudy/StagingCaseStudy";
